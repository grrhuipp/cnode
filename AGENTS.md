# AGENTS

本文件只记录修改 cnode 时必须遵守的规范和约束。实现细节、迁移历史和防回归清单由代码与测试维护，不在这里追加。

- `README.md` 面向使用者，说明定位、配置入口、部署和架构概览。
- `AGENTS.md` 面向贡献者和自动化协作者，说明职责边界、禁止项和删除规则。
- 两份文档冲突时，以本文件为准。不要在两个文件中重复同一内部实现。

## 沟通

- 默认中文回复。
- 不要回退、覆盖或整理用户已有改动，除非用户明确要求。
- 需求不清时只问一个必要问题；能从仓库上下文判断时直接执行。
- 变更前先分析现有代码和配置，再决定是否修改。
- 需要诊断或回归时，优先在当前可用的目标环境完成；无法直连时先说明限制，再给出可在本地完成的分析。
- 默认选择长期正确、边界清晰的一等抽象，即使改动面更大；不要为了缩小 diff 把新能力塞进语义不匹配的旧接口或旁路。

## 请求

- 所有 TCP / UDP 请求只能走唯一热路径：入站处理、分发、出站处理、转发。任意协议都只是 Handler 实现，不拥有独立架构。
- 原生 datagram、UDP-over-TCP 和 Mux/子流只能有服务主链路的窄 helper。helper 不得承载协议私有架构、路由选择、panel 字段或第二条请求链路。
- 协议边界只允许一次虚调用。协议内部不得再套 wrapper、adapter 或第二条请求链路。
- 热路径目标是 0 抽象成本。启动期可以用注册表、工厂和一次虚函数绑定。
- 请求期不得新增 mutex、rwlock、spinlock、共享原子争用、`std::function` 链、多余 `shared_ptr` 分配或 type-erasure。
- 热路径只读取不可变 snapshot，不解析配置，不读取 panel 原始字段。
- 首包按值移交，作为普通上行的第一份数据。实际读端的取消必须覆盖嗅探、路由 DNS、出站拨号、握手和转发。
- 公共 helper 必须有当前生产或测试调用。删除入口时，同步删除只为它服务的实现、状态和 include。

## 配置

- 任意配置来源都必须先归一化，再进入 runtime。热更新只能原子替换 snapshot，不能原地改写运行中的对象图。
- 默认主配置是 `config.json`，目录侧车固定为 `inbounds.json`、`outbounds.json`、`routing.json`。只接受 JSON，不回退其他配置格式，不读取旧 sidecar，legacy 字段不得进入 runtime。
- 配置键只认 camelCase。snake_case 别名直接拒绝。
- 公开配置用可 designated-initialize 的普通聚合。能用字段表达的，不要加 factory 或 builder。
- 每个 receiver 必须在冷路径明确选择强制出口，或带显式 fallback 的路由。策略不可默认构造，空 tag 不得进入热路径。
- Router 只返回真实规则命中。强制出口和入站显式 fallback 由分发层编排。不存在 Worker 全局默认出口，也不得伪装成规则命中。
- 静态 inbound 指定强制出口时不得进入 Router；未指定时参与路由，未命中使用显式 fallback。

## 分层

上层不得下沉，下层不得理解上层产品语义。

| 层级 | 负责 | 不负责 |
| --- | --- | --- |
| inbound | 认证、协议解析、用户识别、目标解析、session metadata、调用分发 | 路由、出站连接、转发、访问出站内部、协议私有长期大缓冲 |
| dispatcher | 只读窄分发策略；编排强制出口或规则命中后的显式 fallback；统一放行或拒绝；查找并调用 outbound | 解析协议、协议特判、执行转发、panel 字段、伪装路由命中、隐式默认出口 |
| router | 只在规则真实命中时给出 outbound tag | 连接、转发、协议、强制出口、fallback、隐式默认出口 |
| request policy | 对统一 session 返回 allow / block | 选路、查找 outbound、解析协议、向分发层暴露面板规则类型 |
| outbound | 拨号或下一跳、出站握手和编码、交给转发 | 路由、读取 panel 原始配置、绕过分发层 |
| relay | 搬运数据和流量打点 | 解析协议、选 outbound、访问 panel、连接级大 scratch、wrapper |
| Worker | 事件循环、accept、本线程能力与分配器 | 协议校验器、具体出站类型、路由决策、panel 同步 |
| proxyman | 按 tag 持有已准备的 handler，receiver 只携带不可变窄策略 | 解析协议或 panel 字段、自己做路由、直接进入转发、暴露跨 Worker 可变 handler |
| transport | 字节传输 | 代理协议解析、选 outbound、panel 配置、协议私有长期 pool |
| control plane | 拉节点、用户和规则，归一化，构建 runtime，原子发布，按面板语义上报 | 进入热路径、改 live handler、访问 Worker 分配器、按协议构建认证凭据 |

## 内存

- 每个 Worker 线程使用自己的 PMR 池。池只服务该线程，不得跨线程共享、移动，也不得在其他线程释放。空块先留在所属线程，短延迟后再还给系统，不等连接结束。
- 在 Worker 线程上创建、并在同一线程销毁的对象，必须由该线程的 PMR 池分配和释放。不得替换全局分配入口来达到这一点。
- 控制面对象、进程退出时的静态对象，以及库内部自行申请的内存，不走该池。
- 禁止在该线程池之外再自建 free-list、缓冲回收桶或进程级共享池。
- 进程层只在启动期初始化，Worker 创建后冻结。热路径不得向共享进程层做普通分配。
- 拥有型容器经当前线程分配器分配和释放。视图只表示借用，不得跨阶段、跨连接或跨 Worker 保存。
- 连接和请求使用的数据面内存在当前阶段结束时整组释放。
- 一条连接的常驻成本只允许：该连接的 socket、维持这条连接所必需的协议会话状态、所属 Worker 的一个调度项，以及一块属于该连接的可复用读缓冲。
- 读缓冲不得每次读新建长期对象，也不得做成跨连接回收池。
- 空闲、读、写超时只能由所属 Worker 的一个调度器承载。禁止每条连接常驻定时器、超时协程或堆定时节点。
- 空闲连接不得继续持有 scratch、数据面缓冲，或整份文件与日志内容。

## Worker

- 运行时由一个主控 Worker 和固定数量的数据 Worker 组成。主控 Worker 使用主线程的唯一事件循环，统一运行面板、监控和 DNS；DNSWorker 不再另建线程或事件循环。
- 每个数据 Worker 同构：同一份 runtime 配置、同一组监听、同一套入站、出站和路由契约。
- 一个数据 Worker 只有一个线程、一个事件循环和一组 `SO_REUSEPORT` 监听。Windows 明确允许降级为 `SO_REUSEADDR`，不保证等价负载分配；所有数据 Worker 仍各自绑定、接收并拥有连接。监听数量不得乘增 Worker 或数据资源。
- 数据 Worker 没有 leader、没有特殊的 0 号 Worker；主控 Worker 不接受或转发代理连接。
- 连接由接受它的 Worker 终身拥有，直到关闭。不得跨线程迁移，也不得按哈希、轮询、固定下标或取第一个 Worker 改派。
- 不得用回调、内存转发或连接移交绕过接受该连接的 Worker。
- Worker 的 live 能力由同一运行时对象统一构造、启动、暴露和关闭，不得散落成相互独立的生命周期。
- Worker 集合在启动期确定，运行时不能增删。进入运行后，Worker 存活至进程退出。
- 每个数据 Worker 独立拥有自己的入站、出站、路由、分发、策略、连接跟踪、在线设备、流量计数、限速、连接限制和分配器。这些对象不得跨数据 Worker 共享。DNS 是明确的例外：主控 Worker 的 DNSWorker 组件独占 DNS 查询、在途请求、socket、超时调度和缓存；数据 Worker 与面板通过有界投递传入自有域名，仅接收按值返回的结果，不直接访问其内部状态。

进程级只允许共享启动后只读的快照，以及明确的有界冷路径对象：

- 认证用户快照
- 主控 Worker 上 DNSWorker 的有界请求入口；其 live 状态仅由主控线程操作
- 启动后只读的 geo 数据
- 属于该 Worker 的统计分片
- 访问日志的有界内存队列；事件按值拷贝，不持有 Worker 指针
- 主控 Worker 所在的事件循环（控制面、监控与 DNS 共用）

不允许：

- 跨线程或跨 Worker 直接读写 Worker 私有对象，即使看起来无锁或只有 atomic
- 把 handler、监听、流、会话、分配器或缓冲提供者的指针或引用保存到其他 Worker
- 跨 Worker 直接读写 DNS、缓存、在途解析、UDP socket 或超时调度；仅允许通过主控 Worker 上 DNSWorker 的有界请求入口取得结果
- 在全局只读快照上做热路径写
- 为跨线程访问热路径状态临时加锁
- 用无界投递或裸协程启动作为进入 Worker 的控制面入口
- 用共享原子或无锁结构保存 live handler、manager、连接或缓冲池，以绕过 Worker 所有权

跨线程只能：

- 经所属 Worker 的有界邮箱投递；满则拒绝，不得无界排队
- 投递的工作必须自己拥有状态，不得依赖调用方的临时对象或 Worker 私有对象
- 复制或发布不可变 snapshot
- 冷路径同步

普通转发不得改走跨线程写队列、共享缓冲或多态写节点。

## 用户与 DNS

- 用户只有一条链路：面板用户，到协议构建，到用户集合，到进程级只读快照，到入站只读认证。
- 禁止第二套用户表，禁止在控制面、分发、路由、转发或 Worker 中按协议构建认证凭据。
- 在线设备、连接数和限设备是 Worker 私有状态。
- 用户快照按协议和 tag 发布。发布失败保留原快照。
- DNS 查询、在途请求、socket、缓存及其超时调度只属于主控 Worker 的 DNSWorker 组件。数据 Worker 只持有有界投递客户端；取消请求必须停止等待并收束投递任务。
- 缓存写入失败不得丢失已经取得的解析结果。

## 控制面

- 面板资源标签使用保留的 `panel/` 前缀，且必须包含面板名称和节点 ID。
- 端点准入在控制面完成。不能靠各 Worker 竞争监听来决定节点所有权。
- 同一 Worker 内，新监听不得与其他 tag 的地址和端口重叠。IPv6 固定 v6-only。
- 候选快照和回滚凭据必须在变更前构建完成。未完成清理不得开始新变更。
- 面板地址回退只允许发生在连接和 TLS 握手阶段。开始发送后不得换地址重发。
- 控制面批量操作必须等待已启动的子任务全部结束。
- 面板同步和状态心跳必须使用独立循环。状态循环只读最后提交的节点状态，失败单独报告。

## 删除

发现以下情况时，不做兼容，不做转接，直接删除、合并或下沉：

1. 公开 wrapper 或兼容层。
2. 协议流对象进入请求链路。
3. Worker 直接调用协议校验器。
4. 分发层依赖具体协议、接收器配置或面板策略类型。
5. 路由层执行转发，或保存强制出口、fallback 和隐式默认出口。
6. 转发层解析协议。
7. 面板修改 live handler，或出站读取面板原始字段。
8. 热路径解析配置。
9. 连接常驻大 scratch，或协议私有长期池。
10. 同一 manager 承担多个层级的职责。
11. legacy 配置进入 runtime。
12. 跨 Worker 直接访问私有对象，或让 Worker 私有指针、引用、分配器、handler、会话或流逃逸。
13. 用无锁或原子共享 live handler、连接或缓冲池。
14. 控制面按协议构建认证凭据，或面板用户绕过唯一用户快照。
15. 绕开主控 Worker 上 DNSWorker 的有界入口，跨线程直接访问其 DNS、在途解析、socket、超时调度或缓存。
16. 子流重入分发时保存完整接收器，而不是窄策略。
17. 出口策略可默认构造、接受空 tag，或回退到第一个 outbound。
18. 跨线程无界进入 Worker。
19. Worker 能力散落成相互独立的生命周期。
20. 每条连接常驻定时器、超时协程或堆定时节点。
21. 空闲连接继续持有数据面缓冲。
22. 已接受的连接被改派到其他 Worker。

## 审查

1. 从请求入口到转发的数据流一眼可见。
2. 任意协议都只是 Handler 实现。删除一个协议不影响分发、路由、转发和 Worker。
3. 任意配置来源都先归一化，再进入 runtime。
4. 数据 Worker 同构、live 状态不共享。主控 Worker 统一拥有控制面、监控和 DNS；DNS live 状态仅在主控线程访问，其他线程仅通过有界入口传值。
5. 任意热路径对象都能指出所属 Worker。
6. 替换一个 panel 不影响协议热路径。
7. 分发层没有接收器配置或面板策略类型。路由无匹配结果为空。
8. 热路径没有额外 wrapper、`std::function` 或共享锁。
9. 每个 Worker 只有本线程 PMR 池。空闲连接不持有数据面缓冲，超时不按连接建定时器。

## 部署

- 更新脚本不带参数时，只更新二进制和 geo 数据。geo 数据先下载到临时目录，失败保留旧文件；内容变化后重启原服务。更新不得改写既有 panel 或 sidecar 配置。
- 符号文件只在显式要求时下载。
- 线上诊断优先使用发布产物，不默认在目标机临时编译。
